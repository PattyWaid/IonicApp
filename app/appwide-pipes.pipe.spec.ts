import { AppwidePipesPipe } from './appwide-pipes.pipe';

describe('AppwidePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new AppwidePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
